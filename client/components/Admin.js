import React from 'react';
import { connect } from 'react-redux';
import { NavLink } from 'react-dom';

const Admin = props => (
  <div className="row">
    <div className="col-6">Welcome to the Admin Dashboard!</div>
  </div>
);

export default Admin;
