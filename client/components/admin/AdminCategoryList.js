import React from 'react';
import { connect } from 'react-redux';

const mapState = state => ({

});

const AdminCategoryList = () => (
  <div>A list of categories</div>
);

export default connect(mapState)(AdminCategoryList);
