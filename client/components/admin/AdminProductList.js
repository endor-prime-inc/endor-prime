import React from 'react';
import { connect } from 'react-redux';

const mapState = state => ({});

const AdminProductList = () => <div>A list of products</div>;

export default connect(mapState)(AdminProductList);
